# Figma Flow Reminder

- Prefer exact frame/layer links with `node-id`.
- `get_design_context` + `get_screenshot` are both required before implementation.
- Reuse project components/tokens before copying generated code literally.
